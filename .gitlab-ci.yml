image: debian:3.6-stretch

pages:
  script:
  - pip install sphinx==1.6.7 numpydoc numpy matplotlib PyQt5 pandas
  - ls
  - cd doc
  - make html
  - cd ..
  - mv doc/_build/html/ public/
  artifacts:
    paths:
    - public

  only:
  - master
